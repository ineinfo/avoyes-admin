import { UserCreateViews } from 'src/sections/category/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Avoyes: Import CSV file',
};

export default function UserCreatePage() {
  return <UserCreateViews />;
}
