import { ProductListView } from 'src/sections/propertytype/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Property type list',
};

export default function ProductListPage() {
  return <ProductListView />;
}
