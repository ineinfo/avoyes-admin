import { UserCreateView } from 'src/sections/color/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Color',
};

export default function UserCreatePage() {
  return <UserCreateView />;
}
