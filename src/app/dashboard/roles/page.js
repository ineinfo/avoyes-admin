import { ProductListView } from 'src/sections/roles/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Roles list',
};

export default function ProductListPage() {
  return <ProductListView />;
}
