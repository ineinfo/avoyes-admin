import { ProductCreateView } from 'src/sections/roles/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new role',
};

export default function ProductCreatePage() {
  return <ProductCreateView />;
}
